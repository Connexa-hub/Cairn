<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base platform theme; Compose (CairnTheme.kt) owns all real theming.
         This just needs to exist for the manifest/launch, and sets the
         AMOLED black window background so there's no flash before Compose draws. -->
    <style name="Theme.Cairn" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>
